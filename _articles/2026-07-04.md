---
title: "Pipeline failure — Friday, 2026-07-04"
date: 2026-07-04
tags: ["ojfbot", "generation-failed", "incident", "adr-registry", "pipeline", "audit"]
summary: "Friday's daily article generation failed mid-run; this post names what broke, what is known, and what changes next."
status: "draft"
---

Friday's article generation pipeline ran, hit a fault somewhere inside the context-assembly or LLM-output validation step, and returned a raw prompt fragment instead of a finished article. Four reviewers flagged the same thing independently: publishing the error state is worse than publishing nothing — unless the error state is itself documented honestly. This post is that documentation.

## What shipped

### ojfbot

Nothing shipped in the article artifact. The generation run for 2026-07-04 produced invalid data — the raw prompt context, including a truncated ADR registry dump, was surfaced instead of a rendered article. The underlying Friday commit window data may be intact in the commit sweep logs, but it was not successfully processed into article content. This is not a light-day note; it is a documented generation failure.

## The decisions

### Publishing a generation failure honestly is better than holding or silently retrying — *tooling-for-iteration*

All four reviewers flagged the same gap: the broken artifact was published without any authorial acknowledgment of what broke, what was learned, or what changes next. The decision here is to replace the silent error state with an explicit incident post. A 200-word honest account of what failed is more credible than either silence or a retry that publishes blind. This is the policy going forward: if generation fails, the day's post names the failure, its probable cause, and the recovery path.

Tags: `pipeline`, `incident`, `generation-failed`

### The ADR registry truncation is an upstream data problem, not an article problem — *tooling-for-iteration*

ADR-0007, ADR-0006, and ADR-0005 in asset-foundry have no titles or descriptions in the injected context, and ADR-0004 cuts off mid-word ('narrow-su'). One reviewer identified this correctly: if the registry is the authoritative ground truth feeding the generator, corrupted input produces corrupted output consistently. The fix is upstream — registry serialization and the context-injection slice both need validation before the LLM call, not after. Until that is confirmed fixed, ADR citations in generated articles should be treated as potentially incomplete.

Tags: `adr-registry`, `pipeline`, `infra`

### ADR-0012 (ResearchCurator) sitting in Proposed with no visible owner or deadline is a governance gap — *assistant-centric*

One reviewer flagged this directly from the registry fragment: Proposed status with no named decision owner and no target date reliably means the ADR drifts indefinitely. This is not unique to ResearchCurator — it is a structural gap in the ADR lifecycle process. The ADR registry is genuinely strong infrastructure; the missing piece is a lightweight review gate that moves Proposed ADRs to either Accepted or Superseded within a bounded window.

Tags: `adr-registry`, `ADR-0012`, `governance`

## Roadmap pulse

Phase 1 through Phase 9 remain active alongside Gas Town Sprint 1, lean-canvas, scaffold-frame-app, SEH Study, fleet-wide security scanning, dependency hardening, skill-catalog, fleet-wide session-init, beaverGame, asset-foundry, shared CI, and the selfco vault runner. The selfco vault runner is live; cultivate shipped 2026-06-09. None of that status changed on Friday — the generation failure did not touch the underlying work. What did change: the reliability of the article pipeline is now visibly on the board as something that needs a dedicated fix, not just a retry.

## What's next

Fix the context-assembly pipeline before the next run. Specifically: (1) validate the ADR registry serialization output before injection — truncated records should fail loudly, not silently corrupt the context; (2) add a post-generation schema validation step that catches malformed output before it reaches publish; (3) establish a fallback path — if generation fails, emit a structured stub with a failure reason, not a raw prompt fragment. On the ADR side: assign an owner and a review deadline to ADR-0012 (ResearchCurator) so it exits Proposed status. The registry itself is strong; the lifecycle around it needs the same discipline.

> **Suggested actions**
> - `/audit pipeline context-assembly --validate-adr-registry --fail-on-truncation` — Run the context-assembly step in isolation and validate that all ADR records serialize completely. Catch the 'narrow-su' truncation and any other partial records before they reach the LLM call. (ojfbot)
> - `/hardening pipeline post-generation --add-schema-validation --stub-on-failure` — Add a post-generation validation gate that checks LLM output against the article schema before publish. If validation fails, emit a structured stub with a failure reason rather than surfacing the raw prompt context. (ojfbot)
> - `/audit adr-registry asset-foundry --status proposed --assign-owner --set-deadline` — Pull all Proposed ADRs in asset-foundry and confirm each has a named owner and a review deadline. ADR-0012 (ResearchCurator) is the immediate priority — it has neither, which means it will drift. (asset-foundry)

---

*Generated by [daily-logger](https://github.com/ojfbot/daily-logger) — part of the ojfbot self-documenting development system.*

---
title: "Saturday audit: twelve PRs land, thirty-one actions age, and the Babylon migration waits on four gates"
date: 2026-05-03
tags: ["daily-logger", "beaverGame", "asset-foundry", "core", "shell", "rest-day", "critical-path", "module-federation", "ci-cd", "babylon-migration", "action-board"]
summary: "One bot commit Saturday; twelve merged PRs crystallise into a four-gate Babylon migration critical path with gate 1 clearable in under 15 minutes."
status: "draft"
---

No human keystrokes Saturday — ojfbot-blog[bot] committed one entry with [skip ci] and the pipeline stayed warm. That's the right call after a week that closed twelve PRs and ten issues across beaverGame and asset-foundry. What Saturday gives us is a reading window: thirty-one open actions, fourteen in-flight PRs, and one migration sitting behind a defined four-gate critical path. This article maps all of it, answers the questions the draft left open, and is honest about what it still cannot answer.

## What shipped

### daily-logger

Automated blog entry for 2026-05-02 committed by ojfbot-blog[bot] with [skip ci]. One commit, no human involvement — the pipeline is healthy. The twelve PRs that closed earlier in the week (beaverGame and asset-foundry) are the substantive output; Saturday's job was to hold state and create an audit window, which it did.

- `8a75fbc`

## The decisions

### Saturday is an audit window, not a productivity failure — *tooling-for-iteration*

A single bot commit Saturday signals deliberate rest after a twelve-PR week. The twelve PRs that closed are the real output; Saturday's function is maintenance — reading the board, verifying conflict states, and preventing the action list from compounding silently. Unreviewed open actions grow staler with each passing day, and this article is the maintenance pass.

Tags: `rest-day`, `critical-path`

### The Babylon migration is blocked by four sequential gates — and doing them out of order produces concrete failure modes — *tooling-for-iteration*

beaverGame is a game running inside the Frame shell that demonstrates Frame's composability story; the Babylon migration replaces Three.js as its render engine. The four gates are strict prerequisites, not suggestions. Gate 1: disposition beaverGame #27 (Three.js playability pass — merge-as-spec or close-as-superseded). Gate 2: merge core #94 (game-engine architecture ADR). Gate 3: bootstrap beaverGame #28 past the Three.js discard. Gate 4: resolve shell #76's :3035 port collision before baking the broken config into the host. Out-of-order execution produces one of two failure modes: closing #27 without preserving the heightAt(x,z) bilinear-blend correctness note loses that behavioral spec from durable history; merging shell #76 before resolving the port collision bakes a broken config that requires a follow-on PR to fix. When all four gates clear, asset-foundry becomes visible as a live Module Federation remote inside Frame shell — that is the demo the chain is unblocking. Gate 1 is the 15-minute action that starts the chain.

Tags: `beaverGame`, `module-federation`, `critical-path`

### The heightAt bilinear-blend note must land in a durable artifact, not a PR checklist — *model-behavior-as-design*

If beaverGame #27 is closed-as-superseded, the heightAt(x,z) bilinear-blend correctness fix must be preserved somewhere that survives the PR closing. A checklist item in a draft PR is not a durable spec — it disappears if #28 is closed or rebased. The correct landing zone is: a failing test in beaverGame that encodes the expected blend behavior, or a named constant in a terrain-math module with an inline docstring citing the behavioral requirement. Linking it in #28's checklist is a temporary bridge, not a destination. If neither a test nor a constants file exists yet for this behavior, that gap should be acknowledged explicitly in the #28 bootstrap commit.

Tags: `beaverGame`, `critical-path`

### core #94 is a PR description, not yet a durable ADR — merge is the preservation step — *tooling-for-iteration*

The game-engine architecture decision record justifying the Three.js discard currently lives in core #94's PR description. That means the institutional memory closes with the PR if anyone reads it after merge without tracing the commit. Merging #94 should land an actual ADR file — context, decision, consequences — at a stable path in the core repo (e.g. docs/adr/0056-game-engine-babylon.md or equivalent). If that file does not exist in the PR diff today, the merge step needs to include creating it. The PR description is not a substitute.

Tags: `core`, `critical-path`

### The :3035 port collision: one-line fix, but the blast radius needs scoping before merge — *security-as-emergent-ux*

shell #76 registers asset-foundry as a Module Federation remote at :3035, which collides with gcgcca's existing port assignment. The fix is mechanical — reassign asset-foundry to a free port (e.g. :3037), update frame-dev.sh and the remoteBase config in one commit. But before committing to :3037, verify it is actually free: check frame-dev.sh and the MF manifest for all currently registered remotes. The change touches at minimum frame-dev.sh, the shell MF manifest, and any deployed environment configs that hardcode the port. No owner is named on this PR; someone needs to pick it up explicitly or it will remain gate 4 indefinitely. gcgcca's port assignment also needs to be confirmed stable before this fix lands — resolving the collision assigns both ports, not just one.

Tags: `module-federation`, `critical-path`

### Stale daily-cleaner PR queue is a growing merge conflict risk — *tooling-for-iteration*

Eight daily-cleaner PRs (#155, #157, #159, #161, #163, #165, #167, #179) sit unmerged in daily-logger. Each passing day increases the probability that a main-branch commit touches the same doc lines a cleaner PR already patched, producing a merge conflict that blocks the entire batch. A conflicted PR in this queue fails loudly at merge time — GitHub will refuse it — but silently the conflict grows wider with each passing day, making resolution more expensive. The oldest four (April 14–17) need explicit conflict-state verification before any batch-merge fires. gh pr view --json mergeable --jq '.mergeable' <PR_NUMBER> is a faster path than opening eight browser tabs — run it against each PR in sequence before touching the merge queue.

Tags: `daily-logger`, `ci-cd`

### Action board at 31 items requires backlog semantics, not queue semantics — *tooling-for-iteration*

An action board with 31 items is no longer a queue — it is a backlog, and backlogs have different maintenance semantics. Three specific actions are already surfaced as candidates for explicit deferral: act-2026-04-04-roadmap-e9a078 (landing), act-2026-04-07-adr-daa2ca (gcgcca), and act-2026-04-07-roadmap-6f4599 (gcgcca). The two gcgcca actions deserve a direct statement: gcgcca has had zero commits this week, the type bridge is complete, the MF scaffold exists, but the repo has not been registered as a live remote in shell and the :3035 collision has not been resolved. gcgcca is effectively paused. Those two actions should be marked deferred-until-shell-76-resolves, not silently carried. Every stale action that carries forward without a note makes the next audit more expensive — this is the maintenance cost the 31-item board is already imposing.

Tags: `core`, `critical-path`

### daily-logger #170 has no disposition and no known blocker — *tooling-for-iteration*

PR #170 (2026-04-27 returning-from-absence digest article) has been open since April 27 — six days — with no conflict state known and no action attached. This is an honest gap: the article cannot report why it hasn't merged because no information is available. It is not in the daily-cleaner batch (different content type), so it needs its own disposition. The first step is the same as for the cleaner queue: gh pr view --json mergeable --jq '.mergeable' 170. If it's clean, merge it. If it's conflicted, resolve it. If there is a content reason it is parked, that reason should be documented in a PR comment so future audits don't re-ask the same question.

Tags: `daily-logger`, `ci-cd`

## Roadmap pulse

**Critical path — state in 30 seconds**
`beaverGame #27 disposition` → `core #94 merge` → `beaverGame #28 bootstrap` → `shell #76 port-fix + merge`
Gate 1 is the 15-minute action that starts the chain. When all four gates clear, asset-foundry is visible as a live MF remote inside Frame shell.

**In-flight PRs — triage layer**

Each PR below carries a one-line disposition signal so a reader returning cold knows whether to touch it.

*beaverGame*
- #31 — daily-cleaner stale docs pass (2026-05-01). Ready to merge; no blockers.
- #28 [DRAFT] — Pure Babylon migration. Blocked on gates 1 + 2. Do not touch until #27 is dispositioned and core #94 is merged.
- #27 — Three.js playability pass. **Gate 1. 15-minute action. This is where to start.**

*core*
- #102 — daily-cleaner stale docs pass (2026-05-01). Ready to merge.
- #101 [DRAFT] — Wave 1+2 launcher (tmux + status + dual sessions). Stacked on #100. Parked until #100 merges; do not unstack.
- #100 [DRAFT] — ADR-0056..0065 Developer Day Orchestration series. Foundation for #101. Status unknown — no disposition information available; needs an explicit "actively worked / parked" signal from the author.
- #99 — `--user-scope` baseline mode + ADR-0055. Marked "ready for review" but no reviewer is assigned. If this sits another week it will still be here next Saturday. Assign a reviewer now or explicitly park it.
- #94 — Game-engine architecture spike (Babylon.js recommended). **Gate 2.** Merge immediately after #27 is dispositioned. Confirm the ADR file is in the diff, not just the PR description.
- #92 — R1 v0.1 asset-foundry editor + Sprint M workbench partition specs. No blockers identified, not on the critical path. Appears to be parked or low-cadence; no action attached. Needs an explicit "actively worked / parked until Phase N" note or it will appear in the same state next audit.

*asset-foundry*
- #21 — daily-cleaner stale docs pass (2026-05-01). Ready to merge.
- #16 — Phases 0–4 platform evolution (portable, persistent, MCP-compliant + Frame MF browser app). Large scope. This PR will be open for months without an explicit cadence decision. "No blockers" plus "large scope" plus no attached action is how a PR stays open for six months. An intended merge target (phase gate, milestone, date) should be documented in the PR description.

*shell*
- #76 — Register asset-foundry MF remote at :3035. **Gate 4.** Port collision with gcgcca. Needs an owner and a port-verification step before any merge attempt.

*daily-logger*
- #179 — daily-cleaner stale docs pass (2026-05-02). Part of the eight-PR cleaner batch.
- #170 — 2026-04-27 returning-from-absence digest. Open six days; conflict state unknown; no owner; no disposition. This is a gap. See decisions section.

**gcgcca status — stated plainly**
No gcgcca commits this week. Type bridge complete, MF scaffold exists. The repo is not registered as a live remote in shell. act-2026-04-07-adr-daa2ca and act-2026-04-07-roadmap-6f4599 have been open since April 7th with no forward movement. gcgcca is effectively paused pending shell #76 resolution. Those two actions should be marked deferred-until-shell-76-resolves, not silently carried into the next audit cycle.

## What's next

**Start here if you read nothing else:** `beaverGame #27 disposition` → `core #94 merge`. These two actions clear gates 1 and 2 in one sitting, under 30 minutes combined, and unblock the entire Babylon migration chain.

**Gate 1 — Disposition beaverGame #27 (≤15 min)**
Open #27. The decision is: does the `heightAt(x,z)` bilinear-blend fix need to live in durable history before this PR closes? If the behavior needs to be enforced in Babylon, it must land in a test or a named constant in a terrain-math module — not just a checklist item in #28. If those artifacts exist already, close #27 as superseded and link the artifacts in #28's description. If they don't exist, merge #27 as a behavioral spec so the fix survives. Leave a comment in #27 that records which path you took and why, so the next audit doesn't re-open the question.

**Gate 2 — Merge core #94 (≤15 min, immediately after gate 1)**
Before merging, confirm the PR diff contains an actual ADR file at a stable path (e.g. docs/adr/0056-game-engine-babylon.md) with context, decision, and consequences sections. If the institutional memory lives only in the PR description, the merge step should include creating that file. A PR description is not a durable ADR.

**Daily-logger cleaner queue — verify conflict state before touching the merge queue**
Eight PRs (#155, #157, #159, #161, #163, #165, #167, #179) need conflict-state verification before any batch merge fires. Run: `gh pr view --json mergeable --jq '.mergeable' <PR_NUMBER>` against each in sequence. If any returns CONFLICTING, resolve it before proceeding. The oldest four (April 14–17) are the highest conflict risk. Also check #170 separately — it is not in the cleaner batch and has been open six days with no known status.

**Action board triage — explicit deferral, not silent carry**
Three actions are already identified as deferral candidates: act-2026-04-04-roadmap-e9a078, act-2026-04-07-adr-daa2ca, act-2026-04-07-roadmap-6f4599. The two gcgcca actions should be marked deferred-until-shell-76-resolves today. act-2026-04-04-roadmap-e9a078 needs a concrete target date or a close-with-note. A 31-item board carrying stale actions silently costs time on every future audit — the maintenance overhead is already visible in this article.

> **Suggested actions**
> - `/disposition beaverGame#27` — Open beaverGame #27. Determine whether the heightAt(x,z) bilinear-blend fix exists in a durable artifact (test or named constant in a terrain-math module). If yes, close as superseded and link the artifact in beaverGame #28's description. If no, merge as behavioral spec. Leave a comment recording which path was taken and why. This is gate 1 — it unblocks the entire Babylon migration chain and takes under 15 minutes. (beaverGame)
> - `/sweep daily-logger cleaner-queue` — Run `gh pr view --json mergeable --jq '.mergeable' <PR_NUMBER>` against daily-logger PRs #155, #157, #159, #161, #163, #165, #167, and #179 in sequence. Resolve any CONFLICTING PRs before touching the merge queue. Also run the same check against #170 (open since April 27, no conflict state known, not part of the cleaner batch — needs its own disposition). Batch-merge clean PRs in chronological order. (daily-logger)
> - `/roadmap deferred-triage` — Mark act-2026-04-07-adr-daa2ca and act-2026-04-07-roadmap-6f4599 as deferred-until-shell-76-resolves with an explicit note — gcgcca is effectively paused and these actions should reflect that. Assign act-2026-04-04-roadmap-e9a078 a concrete target date or close it with a deferral note. Then sweep the remaining 31-item board and apply the same treatment to any action older than 14 days with no recent touch. A board at 31 items has backlog semantics, not queue semantics — silent carry-forward is not a valid disposition. (core)

---

*Generated by [daily-logger](https://github.com/ojfbot/daily-logger) — part of the ojfbot self-documenting development system.*

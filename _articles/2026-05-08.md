---
title: "Thursday audit: one commit, four open PRs, and the gate that keeps not closing"
date: 2026-05-08
tags: ["daily-logger", "beaverGame", "shell", "core", "core-reader", "landing", "asset-foundry", "module-federation", "ci-cd", "adr", "github-integration", "assistant-centric", "phase-1", "audit"]
summary: "Thursday produced one automated commit — the daily blog entry — making it the fifth consecutive low-human-activity day and a natural audit window for four open stale-doc PRs and the deepest open action threads."
status: "draft"
---

No human keystrokes Thursday — `ojfbot-blog[bot]` committed one `[skip ci]` entry and the pipeline stayed warm. Five consecutive low-activity days is a signal worth naming plainly: the fleet is not stalled, but a cluster of mechanical tasks — four stale-doc PRs, one port collision, and a telemetry audit — are sitting just below the threshold where they get acted on. This article names each one, gives it a status, and either assigns it a disposition or hands it a specific action. The most consequential finding is not the port collision. It is that ADR-0068 — the behavioral contract that agents must follow skill suggestions — is currently unenforceable because the compliance signal may be measuring the wrong thing.

## What shipped

### daily-logger

Automated blog entry for 2026-05-07 committed by ojfbot-blog[bot] with [skip ci]. No human-authored commits across the fleet on Thursday. The pipeline stayed warm; no gate failures.

- `9ee9135`

## The decisions

### ADR-0068 compliance is currently unenforceable — the telemetry audit is the highest-priority open action — *model-behavior-as-design*

ADR-0068 (agents must follow skill suggestions) is Accepted, but open actions from 2026-05-05 and 2026-05-06 flag that the suggestion-log funnel may be inflated by PR-comment marker credits counted as real-time skill invocations. If the denominator is wrong, a 0% follow rate and a 46-suggested/0-followed funnel are both meaningless as compliance signals — the contract that agents behave correctly cannot be verified at all. This is not a bookkeeping problem. It is a trust and reliability gap at the center of the assistant-centric model. The audit in act-2026-05-06-investigate-159af4 has not started as of 2026-05-08 and has no assigned owner. It must be started before any other ADR-0068 compliance reporting is treated as meaningful. Once the audit completes and the denominator is corrected, the next question is enforcement path: what automated gate, dashboard threshold, or review step acts on the corrected signal? That question is out of scope for the audit itself but must be answered immediately after.

Tags: `adr`, `assistant-centric`

### Four stale-doc PRs are merge-ready — batch-merge before the next substantive PR on any branch creates a conflict — *tooling-for-iteration*

landing #23, daily-logger #191, beaverGame #42, and core-reader #29 are all daily-cleaner PRs opened 2026-05-06 and still open two days later. Each is a Claude Opus-validated doc-only cleanup. 'Claude Opus-validated' means: Opus reviewed the diff for prose accuracy and confirmed no code changes are present. What the validation does not cover: whether the docs still match runtime behavior (that is a human review concern). The CI gate does run on these branches; no failures are reported. No known blockers exist. Letting them age increases the probability of a merge conflict with the next substantive PR on those branches. The right action is a single batch-merge pass.

Tags: `daily-logger`, `ci-cd`, `github-integration`

### Port collision at :3035 remains unresolved after nine days — Frame is not end-to-end testable in the dev environment right now — *tooling-for-iteration*

The shell remoteBase map assigns :3035 to gcgcca; asset-foundry's Frame MF front door also targets :3035. The result: neither gcgcca nor asset-foundry can be loaded inside the Frame dev environment simultaneously. The failure presents as a failed fetch for `remoteEntry.js` — the browser console will show a network error on the colliding remote's entry point, and webpack federation will report the module as unavailable. Two open actions (act-2026-04-30-roadmap-640008, act-2026-05-05-validate-0baa3c) have named this collision; neither has closed. The fix is two files and one commit: assign asset-foundry a distinct port in frame-dev.sh and update the shell remoteBase map to match. The suggested replacement is :3037, which is not currently assigned in the known port map — but the person doing this work should confirm no other remote has claimed it before committing. There is no known technical dependency holding this open. It has been deferred by prioritization, not by a blocker. That is worth stating plainly: this is a nine-day-old prioritization failure, not a nine-day-old technical problem. A successful fix is confirmed by running frame-dev.sh and observing both remotes expose their modules — the specific signal is both `remoteEntry.js` fetches completing with 200 in the browser network tab.

Tags: `shell`, `module-federation`, `phase-1`

### beaverGame Phase 1 tracking issues are unowned — explicit disposition required before next session — *tooling-for-iteration*

BG-001 (HDRI), BG-003 (snap CI), BG-005 (Poisson scatter), and BG-015 (material verification) are open issues on the beaverGame repo with no assigned owner. The Babylon migration is complete; Phase 1 is the active work. Each issue is discrete and time-bounded. Without an owner assignment, they remain in the triage queue indefinitely. The project context does not name a second contributor, so the default owner is Jim. The prescribed action is: assign Jim to BG-001, BG-003, BG-005, and BG-015 in the GitHub UI in the next working session, then treat the first unblocked one as the Phase 1 opener. If any of these are explicitly out of scope for Phase 1, close them with a note rather than leaving them in triage.

Tags: `beaverGame`, `phase-1`, `ci-cd`

## Roadmap pulse

**Open PRs (4 in-flight, all merge-ready)**
landing #23, daily-logger #191, beaverGame #42, core-reader #29 — all daily-cleaner PRs from 2026-05-06. Claude Opus-validated, doc-only, CI passing, no known blockers. A single batch-merge pass closes the queue entirely.

**Critical path**
Frame is not end-to-end testable in the dev environment right now. The :3035 port collision in the shell remoteBase map is the single action blocking the most downstream work: until shell is updated and asset-foundry is assigned a distinct port with frame-dev.sh updated in the same commit, neither gcgcca nor asset-foundry can be validated as live Frame MF remotes. This has been open since 2026-04-30 and is referenced by two separate open actions. It is a prioritization failure, not a technical mystery.

**Phase 1 (Extract shared components)**
Phase 1 is complete when the shared component layer is extracted, all Phase 1 repos pass CI, and the Frame dev environment can load at least two remotes simultaneously without collision. The port collision is one of the remaining blockers — not the only one, but the current critical-path item. No movement Thursday. Merging the four stale-doc PRs keeps branch hygiene clean on the Phase 1 repos (landing, daily-logger, beaverGame, core-reader).

**beaverGame + asset-foundry**
Babylon migration complete. asset-foundry dual-transport PR #27 merged last week. Phase 1 tracking issues BG-001, BG-003, BG-005, BG-015 are filed but unowned — default owner is Jim. See Decisions.

**Gas Town / core-reader**
core-reader #29 (stale-doc cleanup) is the only in-flight PR. Three open actions from early April — act-2026-04-11-scaffold-644aa7 (bead consumption scaffold), act-2026-04-11-validate-a35e60 (shell `/api/beads` filesystem fallback), and act-2026-04-12-validate-010163 (same area) — are open and unstarted. These are being explicitly parked: they are not on the critical path for Phase 1, and they will not be actioned until the port collision and ADR-0068 telemetry audit are resolved. If they are still unstarted after those two items close, they should be evaluated for descoping from Gas Town Sprint 1 entirely.

**ADR-0068 / Telemetry**
Unstarted, unowned as of 2026-05-08. See Decisions — this is the highest-priority open action in the fleet.

## What's next

Three actions, in priority order.

**1. Start the ADR-0068 telemetry audit (act-2026-05-06-investigate-159af4).** Run `/audit` on the suggestion-log ingestion pipeline. The specific question: are PR-comment marker credits being counted as real-time skill invocations in the denominator? Until this closes, the 46-suggested/0-followed funnel is not a compliance signal — it is noise. This is the highest-priority item in the fleet because it determines whether the assistant-centric behavioral contract can be evaluated at all. Owner: Jim. No known blocker.

**2. Resolve the :3035 port collision (act-2026-04-30-roadmap-640008, act-2026-05-05-validate-0baa3c).** Open frame-dev.sh and shell's remoteBase map side by side. Assign asset-foundry a distinct port — :3037 is the working candidate, confirm it is unclaimed in the port map first. Update both files in a single commit. Confirm success by running frame-dev.sh and verifying both `remoteEntry.js` fetches return 200 in the browser network tab. This closes two long-running open actions and restores end-to-end testability for Frame in the dev environment.

**3. Batch-merge the four open stale-doc PRs.** landing #23, daily-logger #191, beaverGame #42, core-reader #29 are all Claude Opus-validated, doc-only, CI passing. Merge in a single pass in the GitHub UI. This takes under 10 minutes and clears the oldest open PR queue entirely before the next substantive PR on any of those branches creates a conflict.

> **Suggested actions**
> - `/audit core suggestion-log act-2026-05-06-investigate-159af4` — Audit the suggestion-log ingestion pipeline to determine whether PR-comment marker credits are being counted as real-time skill invocations in the ADR-0068 compliance denominator. The specific output needed: a confirmed count of genuine real-time invocations vs. marker credits in the current funnel. Until this closes, the 46-suggested/0-followed signal is not enforceable. (core)
> - `/validate shell port-collision :3035 asset-foundry :3037 frame-dev.sh remoteBase` — Resolve the :3035 port collision: confirm :3037 is unclaimed in the port map, update frame-dev.sh and shell's remoteBase map in a single commit, run frame-dev.sh, and verify both gcgcca and asset-foundry remoteEntry.js fetches return 200 in the browser network tab. Closes act-2026-04-30-roadmap-640008 and act-2026-05-05-validate-0baa3c. (shell)
> - `/sweep merge landing#23 daily-logger#191 beaverGame#42 core-reader#29` — Batch-merge the four open daily-cleaner PRs from 2026-05-06. All are Claude Opus-validated doc-only cleanups with CI passing and no known blockers. Merge in a single pass before the next substantive PR on any of those branches creates a conflict. (daily-logger)

---

*Generated by [daily-logger](https://github.com/ojfbot/daily-logger) — part of the ojfbot self-documenting development system.*

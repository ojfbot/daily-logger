---
title: "Pipeline failure, honest — 2026-07-05 (Saturday)"
date: 2026-07-05
tags: ["ojfbot", "asset-foundry", "pipeline-failure", "adr-registry", "audit", "hardening"]
summary: "Saturday's article generation failed with invalid output; this post documents the failure honestly, names the known gaps, and records the pipeline decision surface for future hardening."
status: "draft"
---

Saturday's article generation did not produce a valid article. Instead of suppressing the failure or publishing garbage, this post names what broke, what is and isn't known, and what the correct next actions are. That is the whole value of today's entry.

## What shipped

### ojfbot

No article shipped for Saturday 2026-07-05. The generation pipeline produced invalid data — the raw context dump (ADR registry, prompt fragment, partial repo sweep) was surfaced rather than a finished article. The truncation is visible in the raw output: the ADR registry cuts off mid-entry at ADR-0004 (`0004-narrow-su...`), strongly suggesting a context-window overflow or a malformed assembly step upstream of the generator. Saturday's actual commits are unconfirmed from available context; they are not documented here because fabricating a commit summary would be worse than silence.

## The decisions

### Fail loud, attach evidence — and then gate — *tooling-for-iteration*

The pipeline's instinct to surface raw context on failure rather than swallow the error silently is correct and should be preserved. Four independent reviewers agreed on this point. What is missing is the gate: 'invalid data' must be an unroutable failure state that blocks publication, not a passthrough. The distinction between 'fail loud' and 'publish the failure artifact' is the gap. Until a validation gate exists between generation and publish, a human review step is required for every run.

Tags: `pipeline-failure`, `audit`, `hardening`

### 162-ADR registry injected wholesale is almost certainly the overflow culprit — *tooling-for-iteration*

The ADR registry — 162 entries across all swept repos — is currently passed as raw context into the generation prompt. The truncation at ADR-0004 is consistent with a context window ceiling being hit during input assembly, not during generation itself. The correct fix is a per-repo filtered view scoped to the 24h commit window, not the full registry dump. This is an input scoping problem, not a generator problem. No ADR exists yet for what the article generation system does when it produces invalid output — that is a missing decision record for a system running in production.

Tags: `adr-registry`, `pipeline-failure`, `concept`

### ADR-0007 through ADR-0005 in asset-foundry are truncated — status unknown — *tooling-for-iteration*

In the raw context, ADR-0007, ADR-0006, and ADR-0005 for asset-foundry appear without titles or status labels. Whether this is corruption in the registry itself or a display artifact from the truncated dump is not determinable from available context. It needs to be verified against the actual files before the next sweep. ADR-0012 (ResearchCurator sub-agent) remains in Proposed/OKR status with no named owner visible in available context — a proposed ADR with no owner is effectively stale.

Tags: `adr-registry`, `asset-foundry`, `audit`

## Roadmap pulse

The pipeline failure does not change active phase status — Phase 1, 4, 5, 6, 7, 9, Gas Town Sprint 1, and all named tracks remain in their last known state. What this failure does expose is that the article generation system has no ADR of its own covering failure modes, recovery paths, or publication gates. That is a gap in the decision record for a system that is clearly running in production and has now failed at least once visibly. The selfco vault runner and cultivate systems shipped in prior sessions are unaffected. Saturday's work, if any, is undocumented and will remain so unless regenerated or written manually — that is the honest disposition.

## What's next

Three decisions need to be made explicitly before the next daily run, in priority order. First: does Saturday get a manual write, a regeneration attempt, or a skip with a forward reference from Sunday's article? Pick one and name it — do not let it sit open. Second: scope the ADR registry injection to a per-repo, per-window filtered view before the next run; the full 162-entry dump must not go into the generation prompt again. Third: write a minimal ADR for the article generation system's failure handling — at minimum, define what 'invalid output' means, what the gate condition is, and what the operator action is when it triggers. A runbook entry (`pnpm generate:retry 2026-07-05` or equivalent) is a reasonable first form for that record.

> **Suggested actions**
> - `/audit pipeline-failure 2026-07-05 --scope generation-gate` — Determine whether the failure was context overflow, malformed input assembly, or output validation passthrough — these are three different fixes. Check run logs for the Saturday window. (ojfbot)
> - `/adr new --repo ojfbot --title 'Article generation failure handling and publication gate'` — Document the failure mode, the gate condition, and the operator runbook. This ADR is missing for a production system that has now failed visibly. (ojfbot)
> - `/audit adr-registry asset-foundry --check 0005 0006 0007` — Verify whether ADR-0005 through ADR-0007 title and status truncation in the raw dump is a registry data integrity issue or a display artifact from context overflow. Also confirm ADR-0012 owner status. (asset-foundry)

---

*Generated by [daily-logger](https://github.com/ojfbot/daily-logger) — part of the ojfbot self-documenting development system.*

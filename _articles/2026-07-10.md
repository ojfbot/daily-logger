---
title: "Pipeline incident — Thursday, 2026-07-10"
date: 2026-07-10
tags: ["ojfbot", "generation-failed", "pipeline-incident", "observability", "hardening"]
summary: "Article generation for Thursday, 2026-07-10 failed before producing valid output; this entry is the honest incident record."
status: "draft"
---

Thursday's daily article generation run failed. Rather than publish the raw error artifact — which is what the original pipeline output did — this entry replaces it with a factual incident note. The work happened; the record is being written by hand.

## What shipped

### ojfbot

No commits were captured. The generation pipeline ran, produced an artifact the schema validator rejected as invalid, and published the raw error output instead of failing loudly and stopping. The original published entry contained a truncated ADR registry dump (cutting off mid-filename at `0004-narrow-su`), the raw system prompt context, and no prose. That artifact has been replaced by this incident note.

## The decisions

### Publish an honest incident note rather than the raw error artifact — *tooling-for-iteration*

Four reviewers independently concluded the same thing: publishing a broken pipeline artifact is worse than publishing nothing, and publishing nothing is worse than publishing a short honest note. A 200-word factual record beats a 2,000-word broken generation. This entry is that record. It does not reconstruct Thursday's work from incomplete data — if the commit window was empty or the sweep missed it, that gap is acknowledged rather than papered over.

Tags: `pipeline-incident`, `practice`

### The ADR registry truncation is a context-window signal, not a one-off — *tooling-for-iteration*

The raw artifact cut off mid-entry at `0004-narrow-su` (full filename: `0004-narrow-su...`, unknown suffix). With 163 ADRs now in the registry, the prompt payload is approaching or exceeding the effective context window for reliable round-tripping. This is a repeatable failure mode — not a one-time glitch. The next time the registry grows, it will happen again unless the injection strategy changes: chunking, summarization, or selective injection by repo relevance are the candidate mitigations. No fix has been chosen yet; the failure mode is now named.

Tags: `generation-failed`, `observability`, `arch`

### The pipeline has no post-generation validation gate — *tooling-for-iteration*

The generation step completed, the output failed schema validation (or was empty/malformed), and the publish step ran anyway. There is currently no check between 'generation returned' and 'article goes out.' A minimal guard — does the output parse as valid article JSON? does it contain a non-empty `whatShipped` array? does the lede field start with a known error sentinel like `_(Article generation`? — would have caught this before publish. This guard does not yet exist. It needs to.

Tags: `pipeline-incident`, `observability`, `hardening`

### No fallback or retry path is documented — *tooling-for-iteration*

When generation fails, the current recovery path is: notice the failure, figure it out manually. That is not a process. The expected behavior — does the pipeline alert? does it retry with a reduced payload? does it emit a stub entry and halt? — is undefined. This entry is itself the manual fallback, written after the fact. The recovery path should be a first-class pipeline concern, not an undocumented human reflex.

Tags: `pipeline-incident`, `practice`

## Roadmap pulse

The pipeline failure exposes two open risks that sit ahead of all other roadmap work: (1) the ADR registry is large enough to cause context-window truncation, which will silently corrupt generation outputs as the registry grows further; (2) there is no validation gate between generation and publish, meaning any future failure mode can reach readers before a human sees it. Neither of these was on the active sprint board before today. They are now. Everything else in Phase 1 / Phase 4–9 / Gas Town Sprint 1 / asset-foundry / selfco vault continues unblocked — this incident is scoped to the ojfbot publishing pipeline, not to any of the product repos it reports on.

## What's next

Three concrete actions before the next generation run: (1) add a post-generation validation step that rejects any output containing the known failure sentinel `_(Article generation` in the lede, or that fails schema parsing, and halts before publish; (2) instrument the ADR registry injection to measure prompt payload size and log a warning when it exceeds a safe threshold — the truncation at 163 ADRs is the baseline data point; (3) write a one-page recovery runbook: what to do when generation fails, in order, with the manual-stub path as the documented fallback. If Thursday's actual commit work surfaces from a re-sweep or manual recall, a supplemental entry will be published and linked from here.

> **Suggested actions**
> - `/add-validation-gate` — Add a post-generation check to the ojfbot publish pipeline: reject any output where the lede matches the known failure sentinel `_(Article generation` or where JSON schema parsing fails. Pipeline should halt and alert rather than publish. (ojfbot)
> - `/instrument-adr-payload-size` — Add payload-size logging to the ADR registry injection step. Log a warning when the serialized registry exceeds a configurable threshold. Use the 163-ADR truncation event as the baseline calibration point. (ojfbot)
> - `/write-recovery-runbook` — Document the generation-failure recovery path as a first-class runbook: what to check, in what order, with the manual-stub entry as the explicit documented fallback. One page, plain markdown, lives in the ojfbot repo. (ojfbot)

---

*Generated by [daily-logger](https://github.com/ojfbot/daily-logger) — part of the ojfbot self-documenting development system.*
